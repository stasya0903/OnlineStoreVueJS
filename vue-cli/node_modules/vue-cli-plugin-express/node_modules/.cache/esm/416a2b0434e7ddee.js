_34e‍.x([["serverDir",()=>serverDir],["commandOptionsDefaults",()=>commandOptionsDefaults],["commandOptionsDoc",()=>commandOptionsDoc],["shouldServeApp",()=>shouldServeApp],["default",()=>_34e‍.o]]);const serverDir = './srv';

       const commandOptionsDefaults = {
  delay: false,
  host: '0.0.0.0',
  port: 3000,
  https: false,
};

       const commandOptionsDoc = {
  '--delay': `delays run by a small duration (default: ${commandOptionsDefaults.delay})`,
  '--host': `specify host (default: ${commandOptionsDefaults.host})`,
  '--port': `specify port (default: ${commandOptionsDefaults.port})`,
  '--https': `use https (default: ${commandOptionsDefaults.https})`,
};

       const shouldServeApp = true;

_34e‍.d({
  serverDir,
  commandOptionsDefaults,
  commandOptionsDoc,
  shouldServeApp,
});
